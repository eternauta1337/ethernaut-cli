{
  "name": "common",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "nyc mocha --exit --timeout 60000 --require 'test/setup.js' --recursive '../../**/*.test.js' --ignore '../../fixture-projects/**/*'"
  },
  "dependencies": {
    "boxen": "^5.1.2",
    "cli-spinners": "^2.9.2",
    "debug": "^4.3.4",
    "spinnies": "^0.5.1"
  },
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "nyc": "^15.1.0"
  }
}
